package io.github.loje0611.tennisdoc.ui.theme

import androidx.compose.ui.graphics.Color

/** 앱 배경 — 깊은 네이비 */
val NavyDeep = Color(0xFF0A1F44)

/** 서페이스(카드 등) — 배경보다 약간 밝은 네이비 */
val NavySurface = Color(0xFF0F2A56)
val NavySurfaceVariant = Color(0xFF152E5C)

/** 테니스 공 에너지 — 네온 그린 */
val NeonGreen = Color(0xFF39FF8A)

/** 일렉트릭 블루 */
val ElectricBlue = Color(0xFF00B4FF)
val ElectricBlueDark = Color(0xFF0088CC)

/** BLE 상태 점 */
val StatusDotDisconnected = Color(0xFF7A8AA3)
val StatusDotScanning = Color(0xFFFFB020)
val StatusDotConnected = NeonGreen

/** 텍스트 */
val TextPrimaryLight = Color(0xFFF0F4FA)
val TextSecondaryLight = Color(0xFFB8C5D9)

/** 스톱 버튼 — 차분한 다크 레드/그레이 */
val StopButtonContainer = Color(0xFF3D2A32)
val StopButtonContent = Color(0xFFE8B4BC)
